<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etoak.system.mapper.DishesMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.etoak.system.entity.Dishes">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="url" property="url" />
        <result column="main" property="main" />
        <result column="main_num" property="mainNum" />
        <result column="minor" property="minor" />
        <result column="minor_num" property="minorNum" />
        <result column="seasoning" property="seasoning" />
        <result column="seasoning_num" property="seasoningNum" />
        <result column="remark" property="remark" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, url, main, main_num, minor, minor_num, seasoning, seasoning_num, remark, create_time, update_time
    </sql>

    <!-- sql 片段，可以被其它标签引用 -->
    <sql id="list_sql">
        select t.id,
               t.name,
               t.url,
               t.main,
               main.name as mainName,
               t.main_num,
               t.minor,
               minor.name as minorName,
               t.minor_num,
               t.seasoning,
               seasoning.name as seasoningName,
               t.seasoning_num,
               t.remark,
               t.create_time,
               t.update_time
        from t_dishes t
     left join t_ingredients main on t.main = main.id and main.type = 1
     left join t_ingredients minor on t.minor = minor.id and minor.type = 2
     left join t_ingredients seasoning on t.seasoning = seasoning.id and seasoning.type = 3
    </sql>

    <select id="getList" parameterType="Dishes" resultType="DishesVO">
        <include refid="list_sql" />
        <where>
            <if test="name != null and name != ''">
                AND t.name like concat(#{name}, '%')
            </if>
            <if test="main != null">
                AND t.main = #{main}
            </if>
            <if test="minor != null">
                AND t.minor = #{minor}
            </if>
            <if test="seasoning != null">
                AND t.seasoning = #{seasoning}
            </if>
        </where>
    </select>

    <select id="listByIds" resultType="DishesVO" parameterType="list">
        <include refid="list_sql" />
        where t.id in
        <foreach item="item" collection="ids" separator="," open="(" close=")">
            #{item}
        </foreach>
    </select>
</mapper>
