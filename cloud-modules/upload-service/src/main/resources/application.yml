server:
  port: 9002

spring:
  application:
    name: upload-service

  config:
    # Data ID:upload-service.yml
    import: nacos:${spring.application.name}.yml

  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: et2512

      config:
        namespace: et2512
