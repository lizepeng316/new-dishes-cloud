server:
  port: 9003

spring:
  application:
    name: auth-service
  config:
    import: nacos:${spring.application.name}.yml

  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: et2512
      config:
        namespace: et2512